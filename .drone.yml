kind: pipeline
name: synbak

environment:
  APP_VER: 3.8.0

trigger:
  event:
  - push
  branch:
  - master

platform:
  os: linux
  arch: noarch

steps:
- name: build
  image: gcc
  commands:
  - ./autogen.sh
  - ./configure
  - make
- name: rpmbuild
  image: rpmbuild:el7
  spec: synbak.spec
